apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: cart-ingress
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`4.182.90.110.nip.io`) && PathPrefix(`/api/cart`)
      kind: Rule
      services:
        - name: cart
          namespace: default
          port: 8002