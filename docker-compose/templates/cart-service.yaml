apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.35.0 (9532ceef3)

  labels:
    io.kompose.service: cart
  name: cart
spec:
  ports:
    - name: "8002"
      port: 8002
      targetPort: 8080
  selector:
    io.kompose.service: cart
