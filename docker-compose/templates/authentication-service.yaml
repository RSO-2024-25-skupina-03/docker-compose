apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.35.0 (9532ceef3)

  labels:
    io.kompose.service: authentication
  name: authentication
spec:
  ports:
    - name: "8003"
      port: 8003
      targetPort: 3000
  selector:
    io.kompose.service: authentication
